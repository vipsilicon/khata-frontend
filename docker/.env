# Host port for the frontend container
FRONTEND_PORT=4000

# Backend API base URL used inside the built Angular app (browser calls this).
# Keep localhost if khata-backend runs on the same machine (host) on port 3000.
# If the backend is on another host, set e.g. http://192.168.1.10:3000
API_BASE_URL=http://localhost:3000
